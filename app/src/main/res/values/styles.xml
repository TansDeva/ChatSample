<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Theme.AppCompat.Light.NoActionBar.FullScreen" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="ViewGroupChat">
        <item name="android:layout_marginBottom">@dimen/chat_margin_v</item>
        <item name="android:layout_marginLeft">@dimen/chat_margin_h</item>
        <item name="android:layout_marginRight">@dimen/chat_margin_h</item>
        <item name="android:layout_marginTop">@dimen/chat_margin_v</item>
        <item name="android:paddingBottom">@dimen/chat_padding_v</item>
        <item name="android:paddingLeft">@dimen/chat_padding_h</item>
        <item name="android:paddingRight">@dimen/chat_padding_h</item>
        <item name="android:paddingTop">@dimen/chat_padding_v</item>
    </style>

</resources>